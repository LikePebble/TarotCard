# P1.1 동기화 마무리 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 로컬 저장이 바뀌면 열려 있는 화면이 갱신되고, 로그인 중이면 그 변경이 Supabase로 흐르게 한다 — `7e6af41`이 남긴 follow-up 3건(push-on-mutation, 저널 서버 동기화, 로그인 병합 후 UI 갱신)을 닫는다.

**Architecture:** `saveStore`/`saveJournal`이 키 있는 변경 이벤트를 emit하는 작은 채널(`local-events.ts`)을 만든다. 화면 훅(`useArcanaStore`/`useJournal`)이 구독해 자동 갱신되고, `layout.tsx`에 놓인 `<SyncBridge/>`가 같은 이벤트를 구독해 2초 디바운스 후 push한다. `store.ts`/`journal.ts`는 Supabase를 import하지 않는다.

**Tech Stack:** Next.js 15 App Router, React 19, `@supabase/supabase-js`, `@supabase/ssr`, Vitest 2 (environment: `node`).

**설계 근거:** [`docs/superpowers/specs/2026-07-22-p1.1-sync-completion-design.md`](../specs/2026-07-22-p1.1-sync-completion-design.md). 결정 번호(S1~S8)는 그 문서의 Decision Log를 가리킨다.

## Global Constraints

- **게스트 불변(S8):** Supabase env가 없거나 세션이 없으면 네트워크 호출이 하나도 일어나지 않아야 한다. `next build`와 유닛테스트가 env 없이 그린으로 유지된다.
- **의존 방향 고정:** `src/lib/store.ts`와 `src/lib/journal.ts`는 **Supabase나 sync 모듈을 import하지 않는다.** 이 두 파일이 아는 것은 `local-events.ts`뿐이다.
- **테스트 환경은 `node`다.** `window`·`localStorage`·`document`가 없다. 새 테스트는 순수 함수와 모듈 스코프 로직만 대상으로 한다. jsdom을 추가하지 않는다.
- **한국어 주석·문구.** 기존 코드의 주석 톤(왜 그런지 설명, 짧게)을 따른다. UI 문구는 한국어.
- **기존 테스트 27개는 전부 통과해야 한다.** `npm test`로 확인한다.
- **로컬 권위(S2):** 로그인 이후 로컬이 권위이고, pull은 로그인 시점 1회뿐이다.
- DRY, YAGNI, TDD, 잦은 커밋.

## File Structure

| 파일 | 책임 |
|---|---|
| `src/lib/local-events.ts` (신규) | 채널별 변경 구독/알림. 의존성 0 |
| `src/lib/local-events.test.ts` (신규) | 위 테스트 |
| `src/lib/store.ts` (수정) | `saveStore` 끝에서 `notifyLocal("store")`; `useArcanaStore`가 구독 |
| `src/lib/journal.ts` (수정) | `saveJournal` 끝에서 `notifyLocal("journal")`; `useJournal`이 구독 |
| `src/lib/sync/merge.ts` (수정) | `mergeJournals` 추가(순수) |
| `src/lib/sync/merge.test.ts` (수정) | `mergeJournals` 테스트 |
| `src/lib/sync/journal-remote.ts` (신규) | `pullRemoteJournal` / `pushLocalJournal` |
| `src/lib/sync/sync.ts` (수정) | `syncJournalOnLogin` 추가 |
| `src/lib/sync/status.ts` (신규) | 동기화 상태(`idle`/`syncing`/`ok`/`error`) + `lastSyncedAt` + 구독 훅 |
| `src/lib/sync/status.test.ts` (신규) | 위 테스트 |
| `src/lib/sync/pusher.ts` (신규) | push 기계(현재 userId, 디바운스 타이머, `flushPendingSync`). React에 의존하지 않는다 |
| `src/components/SyncBridge.tsx` (신규) | 세션 전이를 pusher에 알리고 채널을 구독하는 얇은 컴포넌트. 렌더 없음 |
| `src/app/layout.tsx` (수정) | `<SyncBridge/>` 마운트 |
| `src/lib/auth/session.ts` (수정) | `signOutAndClear` 추가 |
| `src/app/my/AccountCard.tsx` (수정) | 병합 effect 제거(SyncBridge로 이동), 상태 한 줄 표시, 로그아웃 교체 |
| `src/app/my/journal/[date]/page.tsx` (수정) | 중복된 수동 `refresh()` 제거 |

---

### Task 1: 변경 이벤트 채널

**Files:**
- Create: `src/lib/local-events.ts`
- Test: `src/lib/local-events.test.ts`

**Interfaces:**
- Consumes: 없음.
- Produces:
  - `type LocalChannel = "store" | "journal"`
  - `subscribeLocal(ch: LocalChannel, fn: () => void): () => void` — 해지 함수를 반환.
  - `notifyLocal(ch: LocalChannel): void`

- [ ] **Step 1: 실패하는 테스트 작성**

Create `src/lib/local-events.test.ts`:

```ts
import { describe, expect, it, vi } from "vitest";
import { notifyLocal, subscribeLocal } from "@/lib/local-events";

describe("local-events", () => {
  it("구독자에게 알린다", () => {
    const fn = vi.fn();
    const off = subscribeLocal("store", fn);
    notifyLocal("store");
    expect(fn).toHaveBeenCalledTimes(1);
    off();
  });

  it("해지하면 더 이상 알리지 않는다", () => {
    const fn = vi.fn();
    const off = subscribeLocal("store", fn);
    off();
    notifyLocal("store");
    expect(fn).not.toHaveBeenCalled();
  });

  it("채널이 격리된다 - journal 알림은 store 구독자를 깨우지 않는다", () => {
    const onStore = vi.fn();
    const onJournal = vi.fn();
    const offStore = subscribeLocal("store", onStore);
    const offJournal = subscribeLocal("journal", onJournal);
    notifyLocal("journal");
    expect(onStore).not.toHaveBeenCalled();
    expect(onJournal).toHaveBeenCalledTimes(1);
    offStore();
    offJournal();
  });

  it("같은 채널의 여러 구독자에게 모두 알린다", () => {
    const a = vi.fn();
    const b = vi.fn();
    const offA = subscribeLocal("store", a);
    const offB = subscribeLocal("store", b);
    notifyLocal("store");
    expect(a).toHaveBeenCalledTimes(1);
    expect(b).toHaveBeenCalledTimes(1);
    offA();
    offB();
  });

  it("한 구독자가 던져도 나머지 구독자에게 알린다", () => {
    const bad = vi.fn(() => {
      throw new Error("boom");
    });
    const good = vi.fn();
    const offBad = subscribeLocal("store", bad);
    const offGood = subscribeLocal("store", good);
    expect(() => notifyLocal("store")).not.toThrow();
    expect(good).toHaveBeenCalledTimes(1);
    offBad();
    offGood();
  });
});
```

- [ ] **Step 2: 실패를 확인**

Run: `npm test -- src/lib/local-events.test.ts`
Expected: FAIL — `Failed to resolve import "@/lib/local-events"`

- [ ] **Step 3: 최소 구현**

Create `src/lib/local-events.ts`:

```ts
/**
 * 로컬 저장(localStorage)이 바뀌었다는 사실을 알리는 채널.
 *
 * 저장 함수가 여기에 알리고, 화면 훅과 SyncBridge가 각각 구독한다.
 * 덕분에 호출부는 "저장"만 하면 되고 UI 갱신·서버 push를 몰라도 된다.
 * 의존성이 없어야 store/journal이 sync·supabase를 import하지 않는다.
 */
export type LocalChannel = "store" | "journal";

const subscribers: Record<LocalChannel, Set<() => void>> = {
  store: new Set(),
  journal: new Set(),
};

/** 채널을 구독한다. 반환값을 호출하면 해지된다. */
export function subscribeLocal(ch: LocalChannel, fn: () => void): () => void {
  subscribers[ch].add(fn);
  return () => {
    subscribers[ch].delete(fn);
  };
}

/** 채널 구독자에게 변경을 알린다. 한 구독자가 던져도 나머지는 계속 받는다. */
export function notifyLocal(ch: LocalChannel): void {
  for (const fn of [...subscribers[ch]]) {
    try {
      fn();
    } catch (e) {
      console.error("[local-events] 구독자 오류:", e);
    }
  }
}
```

- [ ] **Step 4: 통과를 확인**

Run: `npm test -- src/lib/local-events.test.ts`
Expected: PASS (5 tests)

- [ ] **Step 5: 커밋**

```bash
git add src/lib/local-events.ts src/lib/local-events.test.ts
git commit -m "Add a keyed local-change event channel"
```

---

### Task 2: 저장 시 emit + 화면 훅 구독

**Files:**
- Modify: `src/lib/store.ts` (`saveStore` 함수, `useArcanaStore` 훅)
- Modify: `src/lib/journal.ts` (`saveJournal` 함수, `useJournal` 훅)
- Modify: `src/app/my/journal/[date]/page.tsx:36,67-71` (중복된 수동 refresh 제거)
- Test: `src/lib/store.test.ts` (추가), `src/lib/journal.test.ts` (추가)

**Interfaces:**
- Consumes: Task 1의 `subscribeLocal`, `notifyLocal`, `LocalChannel`.
- Produces: `useArcanaStore()`/`useJournal()`의 반환 형태는 그대로(`{ store, refresh }`). 이제 변경 시 자동 갱신된다.

**중요:** `saveStore`/`saveJournal`의 `notifyLocal` 호출은 `typeof window === "undefined"` 가드 **바깥**(함수 맨 끝)에 둔다. 이유는 두 가지다 — (1) 저장이 실패해도 인메모리 의도는 바뀌었으니 알리는 게 맞고, (2) 테스트 환경이 `node`라 가드 안에 두면 영영 테스트할 수 없다. SSR에서는 `useEffect`가 돌지 않아 구독자가 0이므로 부작용이 없다.

- [ ] **Step 1: 실패하는 테스트 작성**

`src/lib/store.test.ts` 맨 아래에 추가한다(파일 상단 import에 `vi`와 필요한 심볼을 더한다):

```ts
import { emptyStore, setLocalStore } from "@/lib/store";
import { subscribeLocal } from "@/lib/local-events";
import { vi } from "vitest";

describe("setLocalStore", () => {
  it("store 채널로 변경을 알린다", () => {
    const fn = vi.fn();
    const off = subscribeLocal("store", fn);
    setLocalStore(emptyStore());
    off();
    expect(fn).toHaveBeenCalledTimes(1);
  });
});
```

`src/lib/journal.test.ts` 맨 아래에 추가한다:

```ts
import { setEntry } from "@/lib/journal";
import { subscribeLocal } from "@/lib/local-events";
import { vi } from "vitest";

describe("setEntry", () => {
  it("journal 채널로 변경을 알린다", () => {
    const fn = vi.fn();
    const off = subscribeLocal("journal", fn);
    setEntry("2026-07-22", "메모");
    off();
    expect(fn).toHaveBeenCalledTimes(1);
  });

  it("store 채널은 건드리지 않는다", () => {
    const fn = vi.fn();
    const off = subscribeLocal("store", fn);
    setEntry("2026-07-22", "메모");
    off();
    expect(fn).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: 실패를 확인**

Run: `npm test -- src/lib/store.test.ts src/lib/journal.test.ts`
Expected: 새 테스트 3개 FAIL — `expected "spy" to be called 1 times, but got 0 times`

- [ ] **Step 3: `saveStore`에서 emit**

`src/lib/store.ts`의 import에 추가:

```ts
import { notifyLocal, subscribeLocal } from "@/lib/local-events";
```

`saveStore`를 다음으로 교체한다:

```ts
function saveStore(store: ArcanaStore) {
  if (typeof window !== "undefined") {
    try {
      window.localStorage.setItem(STORE_KEY, JSON.stringify(store));
    } catch {
      // storage full or unavailable; reading still works for the session
    }
  }
  // 저장 실패와 무관하게 알린다: 인메모리 의도는 이미 바뀌었다.
  notifyLocal("store");
}
```

- [ ] **Step 4: `useArcanaStore`가 구독**

`src/lib/store.ts`의 `useArcanaStore`를 다음으로 교체한다:

```ts
/** Client hook: null until mounted (SSR-safe), then the live store.
 *  저장이 일어나면(뽑기·로그인 병합·로그아웃 비움) 자동으로 다시 읽는다. */
export function useArcanaStore() {
  const [store, setStore] = useState<ArcanaStore | null>(null);
  const refresh = useCallback(() => setStore(loadStore()), []);
  useEffect(() => {
    refresh();
    return subscribeLocal("store", refresh);
  }, [refresh]);
  return { store, refresh };
}
```

- [ ] **Step 5: `saveJournal`에서 emit + `useJournal`이 구독**

`src/lib/journal.ts`의 import에 추가:

```ts
import { notifyLocal, subscribeLocal } from "@/lib/local-events";
```

`saveJournal`을 교체한다:

```ts
function saveJournal(store: JournalStore) {
  if (typeof window !== "undefined") {
    try {
      window.localStorage.setItem(KEY, JSON.stringify(store));
    } catch {
      // storage full/unavailable; in-memory value still usable this session
    }
  }
  // 저장 실패와 무관하게 알린다: 인메모리 의도는 이미 바뀌었다.
  notifyLocal("journal");
}
```

`useJournal`을 교체한다:

```ts
/** Client hook: null until mounted (SSR-safe), then the live journal.
 *  일기가 저장되거나 로그인 병합이 끝나면 자동으로 다시 읽는다. */
export function useJournal() {
  const [store, setStore] = useState<JournalStore | null>(null);
  const refresh = useCallback(() => setStore(loadJournal()), []);
  useEffect(() => {
    refresh();
    return subscribeLocal("journal", refresh);
  }, [refresh]);
  return { store, refresh };
}
```

- [ ] **Step 6: 통과를 확인**

Run: `npm test`
Expected: PASS — 30 tests (기존 27 + 신규 3), 5 files

- [ ] **Step 7: 중복된 수동 refresh 제거**

`src/app/my/journal/[date]/page.tsx`에서 이제 `setEntry`가 자동으로 갱신을 유발하므로 수동 호출을 지운다.

36번 줄을 교체:

```ts
  const { store: journal } = useJournal();
```

67~71번 줄의 `save`를 교체:

```ts
  const save = () => {
    setEntry(date, body);
    setSaved(true);
  };
```

- [ ] **Step 8: 타입체크·빌드 확인**

Run: `npx tsc --noEmit`
Expected: 오류 없음

Run: `npm run build`
Expected: 성공(`Compiled successfully`)

- [ ] **Step 9: 커밋**

```bash
git add src/lib/store.ts src/lib/journal.ts src/lib/store.test.ts src/lib/journal.test.ts "src/app/my/journal/[date]/page.tsx"
git commit -m "Refresh open screens whenever local storage changes"
```

---

### Task 3: `mergeJournals` (순수)

**Files:**
- Modify: `src/lib/sync/merge.ts`
- Test: `src/lib/sync/merge.test.ts`

**Interfaces:**
- Consumes: `JournalStore`, `JournalEntry` from `@/lib/journal`.
- Produces: `mergeJournals(local: JournalStore, remote: JournalStore): JournalStore`

규칙(S3): 날짜가 양쪽에 있으면 `updatedAt` 문자열 비교로 큰 쪽을 택하고, **같으면 local**을 택한다(S2 로컬 권위). 한쪽에만 있으면 그대로 채택한다. `updatedAt`은 항상 `toISOString()` 결과라 사전식 비교가 시간순 비교와 일치한다.

- [ ] **Step 1: 실패하는 테스트 작성**

`src/lib/sync/merge.test.ts` 맨 아래에 추가한다(상단 import에 `mergeJournals`와 `JournalStore` 타입을 더한다):

```ts
import { mergeJournals } from "@/lib/sync/merge";
import type { JournalStore } from "@/lib/journal";

describe("mergeJournals", () => {
  it("한쪽에만 있는 날짜는 그대로 채택한다", () => {
    const local: JournalStore = {
      "2026-07-20": { body: "로컬만", updatedAt: "2026-07-20T01:00:00.000Z" },
    };
    const remote: JournalStore = {
      "2026-07-21": { body: "서버만", updatedAt: "2026-07-21T01:00:00.000Z" },
    };
    const merged = mergeJournals(local, remote);
    expect(merged["2026-07-20"].body).toBe("로컬만");
    expect(merged["2026-07-21"].body).toBe("서버만");
  });

  it("양쪽에 있으면 updatedAt이 최신인 쪽을 택한다", () => {
    const local: JournalStore = {
      "2026-07-20": { body: "낡은 로컬", updatedAt: "2026-07-20T01:00:00.000Z" },
    };
    const remote: JournalStore = {
      "2026-07-20": { body: "최신 서버", updatedAt: "2026-07-20T09:00:00.000Z" },
    };
    expect(mergeJournals(local, remote)["2026-07-20"].body).toBe("최신 서버");
  });

  it("updatedAt이 같으면 로컬을 택한다", () => {
    const at = "2026-07-20T01:00:00.000Z";
    const local: JournalStore = { "2026-07-20": { body: "로컬", updatedAt: at } };
    const remote: JournalStore = { "2026-07-20": { body: "서버", updatedAt: at } };
    expect(mergeJournals(local, remote)["2026-07-20"].body).toBe("로컬");
  });

  it("빈 스토어끼리 병합하면 빈 스토어다", () => {
    expect(mergeJournals({}, {})).toEqual({});
  });

  it("입력을 변형하지 않는다(순수)", () => {
    const local: JournalStore = {
      "2026-07-20": { body: "로컬", updatedAt: "2026-07-20T01:00:00.000Z" },
    };
    const remote: JournalStore = {
      "2026-07-20": { body: "서버", updatedAt: "2026-07-20T09:00:00.000Z" },
    };
    mergeJournals(local, remote);
    expect(local["2026-07-20"].body).toBe("로컬");
    expect(remote["2026-07-20"].body).toBe("서버");
  });
});
```

- [ ] **Step 2: 실패를 확인**

Run: `npm test -- src/lib/sync/merge.test.ts`
Expected: FAIL — `mergeJournals is not a function` 또는 import 오류

- [ ] **Step 3: 최소 구현**

`src/lib/sync/merge.ts` 상단 import에 추가:

```ts
import type { JournalStore } from "@/lib/journal";
```

파일 맨 아래에 추가:

```ts
/**
 * 일기 병합: 날짜별 last-write-wins(updatedAt 기준).
 * 동률이면 로컬을 택한다(로그인 이후 로컬이 권위).
 * updatedAt은 항상 ISO 문자열이라 사전식 비교 = 시간순 비교.
 */
export function mergeJournals(
  local: JournalStore,
  remote: JournalStore,
): JournalStore {
  const merged: JournalStore = { ...remote };
  for (const [date, entry] of Object.entries(local)) {
    const other = merged[date];
    if (!other || entry.updatedAt >= other.updatedAt) merged[date] = entry;
  }
  return merged;
}
```

- [ ] **Step 4: 통과를 확인**

Run: `npm test`
Expected: PASS — 35 tests

- [ ] **Step 5: 커밋**

```bash
git add src/lib/sync/merge.ts src/lib/sync/merge.test.ts
git commit -m "Add mergeJournals with last-write-wins by updatedAt"
```

---

### Task 4: 저널 원격 왕복 + 로그인 시 저널 병합

**Files:**
- Create: `src/lib/sync/journal-remote.ts`
- Modify: `src/lib/sync/sync.ts`

**Interfaces:**
- Consumes: `getBrowserSupabase` from `@/lib/supabase/client`, `mergeJournals` (Task 3), `loadJournal`/`setLocalJournal` from `@/lib/journal`.
- Produces:
  - `pullRemoteJournal(userId: string): Promise<JournalStore | null>` — 미설정/실패면 `null`.
  - `pushLocalJournal(userId: string, store: JournalStore): Promise<void>`
  - `syncJournalOnLogin(userId: string): Promise<void>`
  - `setLocalJournal(store: JournalStore): void` — `src/lib/journal.ts`에 추가.

DB 테이블은 `public.journal_entries`이고 컬럼은 `user_id`, `entry_date`(date), `body`(text), `updated_at`(timestamptz), PK는 `(user_id, entry_date)`다.

**삭제 규칙(S4):** push는 upsert를 **먼저** 하고, 그 다음 로컬에 없는 서버 행을 지운다. 순서를 뒤집으면 중간 실패 시 서버가 비는 창이 생긴다. 단, **로컬이 비어 있으면 delete를 하지 않는다** — `loadJournal()`은 localStorage 파손 시에도 `{}`를 반환하므로, 파손 한 번이 백업 전체를 지우는 것을 막는다.

- [ ] **Step 1: `setLocalJournal` 추가**

`src/lib/journal.ts`의 `setEntry` 아래에 추가한다:

```ts
/** 병합/동기화 결과를 로컬 일기에 반영한다(부작용). */
export function setLocalJournal(store: JournalStore): void {
  saveJournal(store);
}
```

- [ ] **Step 2: 원격 왕복 구현**

Create `src/lib/sync/journal-remote.ts`:

```ts
import { getBrowserSupabase } from "@/lib/supabase/client";
import type { JournalStore } from "@/lib/journal";

type JournalRow = {
  entry_date: string; // YYYY-MM-DD
  body: string;
  updated_at: string; // ISO
};

/** 서버에서 유저의 일기를 읽어 date -> entry 맵으로. 미설정/실패면 null. */
export async function pullRemoteJournal(
  userId: string,
): Promise<JournalStore | null> {
  const supabase = getBrowserSupabase();
  if (!supabase) return null;
  const { data, error } = await supabase
    .from("journal_entries")
    .select("entry_date, body, updated_at")
    .eq("user_id", userId);
  if (error || !data) {
    if (error) console.error("[sync] 일기 pull 실패:", error.message);
    return null;
  }
  const store: JournalStore = {};
  for (const row of data as JournalRow[]) {
    store[row.entry_date] = {
      body: row.body,
      updatedAt: new Date(row.updated_at).toISOString(),
    };
  }
  return store;
}

/**
 * 로컬 일기를 서버에 반영한다. 미설정이면 no-op.
 * upsert 먼저, 그 다음 로컬에 없는 서버 행 삭제(로컬이 권위).
 * 로컬이 비어 있으면 삭제하지 않는다 — 파손된 localStorage가 백업을 지우지 못하게.
 */
export async function pushLocalJournal(
  userId: string,
  store: JournalStore,
): Promise<void> {
  const supabase = getBrowserSupabase();
  if (!supabase) return;

  // 날짜 형식을 검증한다. 아래 delete 필터는 문자열로 조립되므로,
  // 파손된 localStorage의 임의 키가 필터에 섞여 들어가면 안 된다.
  const dates = Object.keys(store).filter((d) => /^\d{4}-\d{2}-\d{2}$/.test(d));
  if (dates.length > 0) {
    const rows = dates.map((date) => ({
      user_id: userId,
      entry_date: date,
      body: store[date].body,
      updated_at: store[date].updatedAt,
    }));
    const { error } = await supabase
      .from("journal_entries")
      .upsert(rows, { onConflict: "user_id,entry_date" });
    if (error) {
      console.error("[sync] 일기 push 실패:", error.message);
      return; // upsert가 실패했으면 삭제도 하지 않는다.
    }

    const { error: delError } = await supabase
      .from("journal_entries")
      .delete()
      .eq("user_id", userId)
      .not("entry_date", "in", `(${dates.map((d) => `"${d}"`).join(",")})`);
    if (delError) console.error("[sync] 일기 정리 실패:", delError.message);
  }
}
```

- [ ] **Step 3: `syncJournalOnLogin` 추가**

`src/lib/sync/sync.ts`를 다음 내용으로 교체한다:

```ts
import { loadStore, setLocalStore } from "@/lib/store";
import { loadJournal, setLocalJournal } from "@/lib/journal";
import { mergeJournals, mergeStores } from "@/lib/sync/merge";
import { pullRemoteStore, pushLocalStore } from "@/lib/sync/remote";
import {
  pullRemoteJournal,
  pushLocalJournal,
} from "@/lib/sync/journal-remote";

/**
 * 로그인 시 게스트→계정 병합.
 * 서버 pull → 로컬과 병합(리딩 union·도감 재계산) → 병합본을 로컬·서버에 반영.
 * 미설정이면 pull이 null이라 로컬을 그대로 두고 끝난다(안전한 no-op).
 */
export async function syncOnLogin(userId: string): Promise<void> {
  const remote = await pullRemoteStore(userId);
  const local = loadStore(); // 네트워크 왕복 후 최신 로컬을 읽는다(왕복 중 기록 유실 방지).
  const merged = remote ? mergeStores(local, remote) : local;
  setLocalStore(merged);
  await pushLocalStore(userId, merged);
}

/**
 * 로그인 시 일기 병합. 날짜별 last-write-wins.
 * 병합본은 서버의 상위집합이므로, 이때의 push는 서버 행을 지우지 않는다.
 */
export async function syncJournalOnLogin(userId: string): Promise<void> {
  const remote = await pullRemoteJournal(userId);
  const local = loadJournal();
  const merged = remote ? mergeJournals(local, remote) : local;
  setLocalJournal(merged);
  await pushLocalJournal(userId, merged);
}
```

- [ ] **Step 4: 타입체크**

Run: `npx tsc --noEmit`
Expected: 오류 없음

- [ ] **Step 5: 기존 테스트가 깨지지 않았는지 확인**

Run: `npm test`
Expected: PASS — 35 tests

- [ ] **Step 6: 커밋**

```bash
git add src/lib/sync/journal-remote.ts src/lib/sync/sync.ts src/lib/journal.ts
git commit -m "Sync journal entries to Supabase on login and on push"
```

---

### Task 5: 동기화 상태 모듈

**Files:**
- Create: `src/lib/sync/status.ts`
- Test: `src/lib/sync/status.test.ts`

**Interfaces:**
- Consumes: 없음(React `useEffect`/`useState`만).
- Produces:
  - `type SyncState = "idle" | "syncing" | "ok" | "error"`
  - `getSyncStatus(): { state: SyncState; lastSyncedAt: string | null }`
  - `setSyncState(state: SyncState): void` — `"ok"`이면 `lastSyncedAt`을 지금으로 갱신하고 localStorage(`arcana.sync.lastAt`)에 남긴다. `"idle"`은 리셋(`lastSyncedAt`도 null).
  - `subscribeSyncStatus(fn: () => void): () => void` — 상태 변화 구독. 해지 함수를 반환.
  - `useSyncStatus(): { state: SyncState; lastSyncedAt: string | null }` — 클라이언트 훅.

`local-events`를 재사용하지 않고 자체 구독자 집합을 둔다. 동기화 상태는 "로컬 저장이 바뀌었다"와 의미가 다르고, 여기에 섞으면 상태 변화가 화면 스토어 재읽기를 유발한다.

- [ ] **Step 1: 실패하는 테스트 작성**

Create `src/lib/sync/status.test.ts`:

```ts
import { beforeEach, describe, expect, it, vi } from "vitest";
import {
  getSyncStatus,
  setSyncState,
  subscribeSyncStatus,
} from "@/lib/sync/status";

describe("sync status", () => {
  beforeEach(() => {
    setSyncState("idle");
  });

  it("상태를 기록하고 돌려준다", () => {
    setSyncState("syncing");
    expect(getSyncStatus().state).toBe("syncing");
  });

  it("ok이면 lastSyncedAt을 채운다", () => {
    expect(getSyncStatus().lastSyncedAt).toBeNull();
    setSyncState("ok");
    expect(getSyncStatus().lastSyncedAt).not.toBeNull();
  });

  it("error는 lastSyncedAt을 지우지 않는다", () => {
    setSyncState("ok");
    const at = getSyncStatus().lastSyncedAt;
    setSyncState("error");
    expect(getSyncStatus().lastSyncedAt).toBe(at);
    expect(getSyncStatus().state).toBe("error");
  });

  it("구독자에게 상태 변화를 알린다", () => {
    const fn = vi.fn();
    const off = subscribeSyncStatus(fn);
    setSyncState("syncing");
    off();
    expect(fn).toHaveBeenCalled();
  });
});
```

`beforeEach`의 `setSyncState("idle")`이 `lastSyncedAt`을 지워야 두 번째 테스트가 성립한다. 구현에서 `"idle"`은 리셋으로 취급한다.

- [ ] **Step 2: 실패를 확인**

Run: `npm test -- src/lib/sync/status.test.ts`
Expected: FAIL — `Failed to resolve import "@/lib/sync/status"`

- [ ] **Step 3: 구현**

Create `src/lib/sync/status.ts`:

```ts
"use client";

import { useEffect, useState } from "react";

export type SyncState = "idle" | "syncing" | "ok" | "error";

export type SyncStatus = {
  state: SyncState;
  /** 마지막으로 push가 성공한 시각(ISO). 한 번도 없었으면 null. */
  lastSyncedAt: string | null;
};

const LAST_AT_KEY = "arcana.sync.lastAt";

function readLastAt(): string | null {
  if (typeof window === "undefined") return null;
  try {
    return window.localStorage.getItem(LAST_AT_KEY);
  } catch {
    return null;
  }
}

let status: SyncStatus = { state: "idle", lastSyncedAt: readLastAt() };
const subscribers = new Set<() => void>();

export function getSyncStatus(): SyncStatus {
  return status;
}

export function subscribeSyncStatus(fn: () => void): () => void {
  subscribers.add(fn);
  return () => {
    subscribers.delete(fn);
  };
}

/** 상태를 바꾸고 구독자에게 알린다. "ok"이면 lastSyncedAt을 지금으로. "idle"은 리셋. */
export function setSyncState(state: SyncState): void {
  if (state === "idle") {
    status = { state, lastSyncedAt: null };
  } else if (state === "ok") {
    const at = new Date().toISOString();
    status = { state, lastSyncedAt: at };
    if (typeof window !== "undefined") {
      try {
        window.localStorage.setItem(LAST_AT_KEY, at);
      } catch {
        // 표시용 값일 뿐이라 실패해도 무시한다.
      }
    }
  } else {
    status = { state, lastSyncedAt: status.lastSyncedAt };
  }
  for (const fn of [...subscribers]) {
    try {
      fn();
    } catch (e) {
      console.error("[sync-status] 구독자 오류:", e);
    }
  }
}

/** Client hook: 현재 동기화 상태. */
export function useSyncStatus(): SyncStatus {
  const [value, setValue] = useState<SyncStatus>({
    state: "idle",
    lastSyncedAt: null,
  });
  useEffect(() => {
    setValue(getSyncStatus());
    return subscribeSyncStatus(() => setValue(getSyncStatus()));
  }, []);
  return value;
}
```

- [ ] **Step 4: 통과를 확인**

Run: `npm test`
Expected: PASS — 39 tests

- [ ] **Step 5: 커밋**

```bash
git add src/lib/sync/status.ts src/lib/sync/status.test.ts
git commit -m "Add an observable sync status with last-synced timestamp"
```

---

### Task 6: push 기계 + SyncBridge

**Files:**
- Create: `src/lib/sync/pusher.ts`
- Create: `src/components/SyncBridge.tsx`
- Modify: `src/app/layout.tsx`
- Modify: `src/app/my/AccountCard.tsx` (병합 effect 제거)

**Interfaces:**
- Consumes: `useSession` (`@/lib/auth/session`), `subscribeLocal` (Task 1), `syncOnLogin`/`syncJournalOnLogin` (Task 4), `setSyncState` (Task 5), `loadStore`/`loadJournal`, `pushLocalStore`/`pushLocalJournal`.
- Produces (`pusher.ts`):
  - `setSyncUser(userId: string | null): void` — 세션 전이를 알린다. 새 id면 로그인 병합을 실행한다.
  - `schedulePush(): void` — 2초 디바운스 push 예약.
  - `flushPendingSync(timeoutMs?: number): Promise<void>` — 대기분을 즉시 올린다. Task 7이 로그아웃 직전에 부른다.
- Produces (`SyncBridge.tsx`): `<SyncBridge />` — 렌더 결과 없음(`null`).

**왜 두 파일로 나누는가:** `session.ts`는 로그아웃에서 `flushPendingSync`가 필요하고, `SyncBridge`는 `session.ts`의 `useSession`이 필요하다. push 기계가 컴포넌트 파일에 있으면 **순환 import**가 된다. React에 의존하지 않는 `pusher.ts`로 분리하면 의존이 한 방향으로 흐른다:
`SyncBridge → pusher`, `session → pusher`, `SyncBridge → session`.

동작:
1. 세션이 `null → {id}`로 전이하면 병합 2종을 실행한다. **지금 `AccountCard`에 있는 이 로직을 여기로 옮긴다** — 현재는 MY 탭을 열어야만 병합이 일어나는 버그다.
2. `store`/`journal` 채널 변경을 받아 2초 디바운스 후 push한다.
3. 자기 자신이 유발한 알림은 무시한다(`syncing` 플래그). 병합의 `setLocalStore`가 곧바로 중복 push를 예약하는 것을 막는다.
4. `online` 이벤트에서 즉시 push한다(오프라인 복귀 복구).

- [ ] **Step 1: push 기계 구현**

Create `src/lib/sync/pusher.ts`:

```ts
import { loadJournal } from "@/lib/journal";
import { loadStore } from "@/lib/store";
import { setSyncState } from "@/lib/sync/status";
import { syncJournalOnLogin, syncOnLogin } from "@/lib/sync/sync";
import { pushLocalJournal } from "@/lib/sync/journal-remote";
import { pushLocalStore } from "@/lib/sync/remote";

const DEBOUNCE_MS = 2000;

/** 로그인 중인 사용자 id. 없으면 push 자체를 하지 않는다(게스트). */
let currentUserId: string | null = null;
/** 이미 병합을 끝낸 사용자 id. 같은 세션에서 두 번 병합하지 않는다. */
let mergedFor: string | null = null;
/** 대기 중인 디바운스 타이머. */
let pendingTimer: ReturnType<typeof setTimeout> | null = null;
/** 동기화가 진행 중인지. 자기 자신이 유발한 알림을 걸러내는 데 쓴다. */
let syncing = false;

function cancelPending() {
  if (pendingTimer !== null) {
    clearTimeout(pendingTimer);
    pendingTimer = null;
  }
}

async function pushNow(): Promise<void> {
  const userId = currentUserId;
  if (!userId) return;
  syncing = true;
  setSyncState("syncing");
  try {
    await pushLocalStore(userId, loadStore());
    await pushLocalJournal(userId, loadJournal());
    setSyncState("ok");
  } catch (e) {
    console.error("[sync] push 중 예외:", e);
    setSyncState("error");
  } finally {
    syncing = false;
  }
}

/** 2초 디바운스 push 예약. 동기화 중이거나 게스트면 아무 일도 하지 않는다. */
export function schedulePush(): void {
  if (!currentUserId || syncing) return;
  cancelPending();
  pendingTimer = setTimeout(() => {
    pendingTimer = null;
    void pushNow();
  }, DEBOUNCE_MS);
}

/**
 * 세션 전이를 알린다. 새 사용자로 로그인하면 게스트→계정 병합을 실행한다.
 * null(로그아웃)이면 대기 중 push를 버리고 상태를 초기화한다.
 */
export function setSyncUser(userId: string | null): void {
  if (!userId) {
    currentUserId = null;
    mergedFor = null;
    cancelPending();
    return;
  }
  currentUserId = userId;
  if (mergedFor === userId) return;
  mergedFor = userId;
  void (async () => {
    syncing = true;
    setSyncState("syncing");
    try {
      await syncOnLogin(userId);
      await syncJournalOnLogin(userId);
      setSyncState("ok");
    } catch (e) {
      console.error("[sync] 로그인 병합 실패:", e);
      setSyncState("error");
    } finally {
      syncing = false;
    }
  })();
}

/**
 * 대기 중인 push를 지금 실행한다(로그아웃 직전 등).
 * timeoutMs 안에 끝나지 않으면 그냥 반환한다 — 호출자를 매달지 않는다(S5a).
 */
export async function flushPendingSync(timeoutMs = 3000): Promise<void> {
  cancelPending();
  if (!currentUserId) return;
  await Promise.race([
    pushNow(),
    new Promise<void>((resolve) => setTimeout(resolve, timeoutMs)),
  ]);
}
```

- [ ] **Step 2: SyncBridge 구현**

Create `src/components/SyncBridge.tsx`:

```tsx
"use client";

import { useEffect } from "react";
import { useSession } from "@/lib/auth/session";
import { subscribeLocal } from "@/lib/local-events";
import { schedulePush, setSyncUser } from "@/lib/sync/pusher";

/**
 * 로컬 변경을 서버로 흘려보내는 다리. UI를 렌더하지 않는다.
 * layout에 한 번만 마운트한다. 게스트(미설정·미로그인)에서는 아무 일도 하지 않는다.
 */
export function SyncBridge() {
  const { user } = useSession();

  // 세션 전이 → 로그인 병합 또는 정리.
  useEffect(() => {
    setSyncUser(user?.id ?? null);
  }, [user]);

  // 로컬이 바뀌면 디바운스 push.
  useEffect(() => {
    const offStore = subscribeLocal("store", schedulePush);
    const offJournal = subscribeLocal("journal", schedulePush);
    return () => {
      offStore();
      offJournal();
    };
  }, []);

  // 온라인 복귀 시 밀린 변경을 한 번에 올린다(전체 멱등 upsert라 이걸로 충분).
  useEffect(() => {
    const onOnline = () => schedulePush();
    window.addEventListener("online", onOnline);
    return () => window.removeEventListener("online", onOnline);
  }, []);

  return null;
}
```

- [ ] **Step 2: layout에 마운트**

`src/app/layout.tsx`의 import에 추가:

```ts
import { SyncBridge } from "@/components/SyncBridge";
```

`<body>` 내부를 교체:

```tsx
      <body className="font-sans antialiased">
        <NoPinchZoom />
        <SyncBridge />
        {children}
      </body>
```

- [ ] **Step 3: AccountCard에서 병합 effect 제거**

`src/app/my/AccountCard.tsx`에서 다음을 지운다:
- 1~10번 줄의 `useEffect`, `useRef` import와 `syncOnLogin` import
- 17번 줄 `const syncedFor = useRef<string | null>(null);`
- 19~29번 줄의 `useEffect` 블록 전체

수정 후 파일 상단은 다음과 같아야 한다:

```tsx
"use client";

import { UserCircle } from "@phosphor-icons/react";
import {
  signInWithProvider,
  signOut,
  useSession,
} from "@/lib/auth/session";

const card =
  "rounded-2xl border border-line bg-ink-1 p-5 lg:rounded-[14px] lg:p-6";

export function AccountCard() {
  const { user, loading, configured } = useSession();

  // Supabase 미설정 → 준비 중(현행 게스트 모드).
  if (!configured) {
```

- [ ] **Step 4: 타입체크·빌드**

Run: `npx tsc --noEmit`
Expected: 오류 없음

Run: `npm run build`
Expected: 성공

- [ ] **Step 5: 테스트가 깨지지 않았는지 확인**

Run: `npm test`
Expected: PASS — 39 tests

- [ ] **Step 6: 커밋**

```bash
git add src/lib/sync/pusher.ts src/components/SyncBridge.tsx src/app/layout.tsx src/app/my/AccountCard.tsx
git commit -m "Push local changes to Supabase from a layout-level SyncBridge"
```

---

### Task 7: 로그아웃 시 flush + 로컬 비움, MY 상태 표시

**Files:**
- Modify: `src/lib/store.ts` (`clearLocalStore` 추가)
- Modify: `src/lib/journal.ts` (`clearLocalJournal` 추가)
- Modify: `src/lib/auth/session.ts` (`signOutAndClear` 추가)
- Modify: `src/app/my/AccountCard.tsx` (로그아웃 버튼 교체, 상태 한 줄)
- Test: `src/lib/store.test.ts`, `src/lib/journal.test.ts` (알림 확인)

**Interfaces:**
- Consumes: `flushPendingSync` (Task 6), `useSyncStatus` (Task 5).
- Produces:
  - `clearLocalStore(): void` — 리딩·도감 키를 지우고 `notifyLocal("store")`.
  - `clearLocalJournal(): void` — 일기 키를 지우고 `notifyLocal("journal")`.
  - `signOutAndClear(): Promise<void>`

**S5a:** flush가 실패하거나 3초를 넘겨도 **로그아웃은 반드시 완주한다.** 로컬을 비우고 세션을 끊는다.

- [ ] **Step 1: 실패하는 테스트 작성**

`src/lib/store.test.ts` 맨 아래에 추가:

```ts
describe("clearLocalStore", () => {
  it("store 채널로 변경을 알린다", () => {
    const fn = vi.fn();
    const off = subscribeLocal("store", fn);
    clearLocalStore();
    off();
    expect(fn).toHaveBeenCalledTimes(1);
  });
});
```

(상단 import에 `clearLocalStore`를 더한다.)

`src/lib/journal.test.ts` 맨 아래에 추가:

```ts
describe("clearLocalJournal", () => {
  it("journal 채널로 변경을 알린다", () => {
    const fn = vi.fn();
    const off = subscribeLocal("journal", fn);
    clearLocalJournal();
    off();
    expect(fn).toHaveBeenCalledTimes(1);
  });
});
```

(상단 import에 `clearLocalJournal`을 더한다.)

- [ ] **Step 2: 실패를 확인**

Run: `npm test -- src/lib/store.test.ts src/lib/journal.test.ts`
Expected: FAIL — `clearLocalStore is not exported` 류의 import 오류

- [ ] **Step 3: 비우기 함수 구현**

`src/lib/store.ts`의 `setLocalStore` 아래에 추가:

```ts
/** 이 기기의 리딩·도감을 지운다(로그아웃). 서버 사본은 건드리지 않는다. */
export function clearLocalStore(): void {
  if (typeof window !== "undefined") {
    try {
      window.localStorage.removeItem(STORE_KEY);
    } catch {
      // 지우지 못해도 알림은 보낸다 — 화면은 다시 읽어 최선의 상태를 보여준다.
    }
  }
  notifyLocal("store");
}
```

`src/lib/journal.ts`의 `setLocalJournal` 아래에 추가:

```ts
/** 이 기기의 일기를 지운다(로그아웃). 서버 사본은 건드리지 않는다. */
export function clearLocalJournal(): void {
  if (typeof window !== "undefined") {
    try {
      window.localStorage.removeItem(KEY);
    } catch {
      // 위와 같다.
    }
  }
  notifyLocal("journal");
}
```

- [ ] **Step 4: 통과를 확인**

Run: `npm test`
Expected: PASS — 41 tests

- [ ] **Step 5: `signOutAndClear` 구현**

`src/lib/auth/session.ts`의 import에 추가:

```ts
import { clearLocalJournal } from "@/lib/journal";
import { clearLocalStore } from "@/lib/store";
import { flushPendingSync } from "@/lib/sync/pusher";
```

파일 맨 아래에 추가:

```ts
/**
 * 로그아웃: 대기 중 push를 먼저 올리고, 이 기기의 기록을 지운 뒤 세션을 끊는다.
 *
 * flush가 실패하거나 늦어도 로그아웃은 완주한다. 네트워크가 없다고 계정에
 * 갇히면 안 되고, 유실 범위는 마지막 성공 push 이후의 델타로 한정된다.
 * 로컬을 비우는 이유는 공용 기기에서 남의 기록이 보이거나 다음 계정에
 * 섞이는 것을 막기 위해서다.
 */
export async function signOutAndClear(): Promise<void> {
  try {
    await flushPendingSync();
  } catch (e) {
    console.error("[sync] 로그아웃 전 flush 실패:", e);
  }
  clearLocalStore();
  clearLocalJournal();
  await signOut();
}
```

- [ ] **Step 6: AccountCard에 상태 한 줄 + 새 로그아웃 연결**

`src/app/my/AccountCard.tsx`의 import를 다음으로 교체:

```tsx
import { UserCircle } from "@phosphor-icons/react";
import {
  signInWithProvider,
  signOutAndClear,
  useSession,
} from "@/lib/auth/session";
import { useSyncStatus } from "@/lib/sync/status";
```

`const card = ...` 아래에 헬퍼를 추가:

```tsx
function syncLine(
  state: "idle" | "syncing" | "ok" | "error",
  lastSyncedAt: string | null,
): string {
  if (state === "syncing") return "동기화 중…";
  if (state === "error") return "동기화 대기 중 · 연결되면 자동으로 올라갑니다";
  if (lastSyncedAt) {
    const d = new Date(lastSyncedAt);
    const hh = String(d.getHours()).padStart(2, "0");
    const mm = String(d.getMinutes()).padStart(2, "0");
    return `마지막 동기화 ${hh}:${mm}`;
  }
  return "기록이 계정에 보관됩니다";
}
```

컴포넌트 본문 첫 줄에 상태를 읽는다:

```tsx
export function AccountCard() {
  const { user, loading, configured } = useSession();
  const { state, lastSyncedAt } = useSyncStatus();
```

로그인된 분기(현재 54~79번 줄)의 이메일 줄 **아래**에 상태 줄을 넣고 버튼을 교체한다. 로그인 분기 전체를 다음으로 교체:

```tsx
  if (user) {
    return (
      <div className={card}>
        <div className="flex items-center justify-between gap-3">
          <span className="flex items-center gap-3.5">
            <UserCircle size={22} className="text-gold-soft" aria-hidden />
            <span>
              <span className="block font-display text-[16px] font-semibold lg:text-[18px]">
                로그인됨
              </span>
              <span className="block text-[13px] text-muted lg:text-[14px]">
                {user.email ?? user.id}
              </span>
              <span className="block text-[12px] text-muted lg:text-[13px]">
                {syncLine(state, lastSyncedAt)}
              </span>
            </span>
          </span>
          <button
            type="button"
            onClick={() => void signOutAndClear()}
            className="text-[13px] text-muted underline underline-offset-4 hover:text-cream"
          >
            로그아웃
          </button>
        </div>
      </div>
    );
  }
```

- [ ] **Step 7: 타입체크·빌드·테스트**

Run: `npx tsc --noEmit`
Expected: 오류 없음

Run: `npm run build`
Expected: 성공

Run: `npm test`
Expected: PASS — 41 tests

- [ ] **Step 8: 커밋**

```bash
git add src/lib/store.ts src/lib/journal.ts src/lib/auth/session.ts src/app/my/AccountCard.tsx src/lib/store.test.ts src/lib/journal.test.ts
git commit -m "Flush and clear local data on logout, surface sync status in MY"
```

---

## 수동 검증 체크리스트 (Supabase 설정 완료 후)

유닛테스트는 순수 로직만 덮는다. 원격 왕복과 `SyncBridge`는 아래를 사람이 확인한다. 선행 조건: Supabase 프로젝트·OAuth·마이그레이션·`.env.local`이 모두 준비돼 있을 것([P1.0 plan](2026-07-19-p1.0-supabase-foundation.md)의 USER SETUP REQUIRED).

- [ ] `.env.local` 없이 `npm run dev` → 카드 뽑기·일기 저장이 되고, 네트워크 탭에 Supabase 요청이 없다(S8).
- [ ] `.env.local` 주입 후 게스트로 리딩 2회 + 일기 1건 작성 → 카카오 로그인 → Supabase `readings`에 2행, `journal_entries`에 1행.
- [ ] 로그인 직후 **MY가 아닌 도감 탭**에서 로그인해도 병합이 일어난다(Task 6 Step 3의 이유).
- [ ] 로그인 상태로 새 리딩 → 2초 후 `readings`에 행 추가, MY에 `마지막 동기화 HH:MM` 갱신.
- [ ] 일기를 수정 → 2초 후 `journal_entries.body` 갱신.
- [ ] 일기 2건 중 1건을 지움 → 2초 후 서버에서도 그 행이 사라진다(S4).
- [ ] 일기를 **전부** 지움 → 서버 행이 남는다(알려진 한계). 아무 일기나 하나 쓰면 정리된다.
- [ ] 기기 오프라인 → 리딩 뽑기 → MY에 `동기화 대기 중` → 온라인 복귀 → 자동으로 올라가고 `마지막 동기화` 갱신.
- [ ] 로그아웃 → 도감·리딩 기록·일기가 화면에서 즉시 비워진다. 재로그인 → 전부 복구된다.
- [ ] 오프라인 상태로 로그아웃 → 3초 안에 로그아웃이 완료된다(매달리지 않는다, S5a).
