# P1.0 Supabase 백엔드 토대 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 로그인 사용자의 데이터를 Supabase에 보관·동기화할 수 있는 토대를 만든다 — DB 스키마, env 가드된 Supabase 클라이언트, 그리고 로컬↔서버 병합 로직. UI 연동(로그인·MY)은 P1.1/P1.2에서 얹는다.

**Architecture:** **로컬 우선 + 서버 동기화.** 기존 localStorage 동기 store API(`loadStore`/`recordReading`/…)를 그대로 유지하고, Supabase는 로그인 시 로컬을 흡수·백업하는 원격 사본으로 둔다. 병합의 정확성은 순수 함수(`mergeStores`)로 보장하고 유닛테스트한다. 도감(collection)은 리딩에서 파생 가능하므로 병합 시 **리딩을 id로 합친 뒤 도감을 재계산**해 이중집계를 원천 차단한다. env(Supabase URL/anon key)가 없으면 클라이언트는 null을 반환하고 앱은 게스트 모드로 정상 동작한다.

**Tech Stack:** Next.js 15 App Router, `@supabase/supabase-js`, `@supabase/ssr`(쿠키 세션), Postgres + RLS, Vitest.

## Global Constraints

- **로컬 우선(스펙 §11):** 서버 권위 리딩 생성은 P2(사주) 몫. P1은 보관/동기화만. 기존 동기 store API를 깨지 않는다.
- **RLS 필수:** 모든 사용자 데이터 테이블은 Row Level Security로 `auth.uid() = user_id`인 행만 접근.
- **env 가드:** `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY`가 없으면 Supabase 클라이언트 팩토리는 `null`을 반환. 앱은 게스트 전용으로 계속 빌드·구동돼야 한다(`next build`, 유닛테스트 그린 유지).
- **시크릿은 저장소에 커밋 금지.** `.env.local`(gitignore됨)에만. `.env.example`로 키 이름만 문서화.
- **도감 = 리딩 파생:** `collection[deckId][slug].count` = 그 덱으로 해당 slug를 만난 리딩 수, `firstAt` = 최초 at. 병합은 리딩 union(id 기준) 후 도감 재계산.
- **리딩 id는 클라 생성 uuid(P0-a).** 서버 upsert의 PK로 재사용해 멱등 동기화.
- DRY, YAGNI(사주·엔타이틀먼트 테이블은 P2/P3에서), TDD(순수 로직), 잦은 커밋.

## USER SETUP REQUIRED (구현 전/병행, 사용자 본인만 가능)

아래는 에이전트가 대신 할 수 없다(계정 생성·자격증명 입력 금지). 검증은 이 설정 완료 후 가능하다:

1. **Supabase 프로젝트 생성**(무료 티어, 리전 `ap-northeast-2` 서울 권장). Project URL + anon key 확보.
2. **Google OAuth**: Google Cloud Console에서 OAuth 클라이언트 생성 → Supabase Dashboard > Authentication > Providers > Google에 Client ID/Secret 입력, redirect URL 등록.
3. **Kakao OAuth**: Kakao Developers에서 앱 생성(REST API 키) → Supabase > Providers > Kakao에 입력, redirect URL 등록. (일부 스코프는 비즈니스 검수 필요할 수 있음)
4. **DB 스키마 적용**: Task 2에서 만드는 `supabase/migrations/0001_p1_foundation.sql`을 Supabase SQL Editor(또는 CLI)로 실행.
5. **env 주입**: 프로젝트 루트 `.env.local`에
   ```
   NEXT_PUBLIC_SUPABASE_URL=...
   NEXT_PUBLIC_SUPABASE_ANON_KEY=...
   ```
   (배포 시 호스팅 환경변수에도 동일 주입.)

## File Structure

- Create `src/lib/supabase/client.ts` — 브라우저 클라이언트 팩토리(env 없으면 null).
- Create `src/lib/supabase/server.ts` — 서버(쿠키) 클라이언트 팩토리(env 없으면 null).
- Create `.env.example` — 키 이름 문서화.
- Create `supabase/migrations/0001_p1_foundation.sql` — 스키마 + RLS + 신규유저 트리거.
- Create `src/lib/sync/merge.ts` — `recomputeCollection`, `mergeStores`(+ 저널 병합). (+ `merge.test.ts`)
- Create `src/lib/sync/remote.ts` — `pullRemoteStore`/`pushLocalStore`(Supabase 왕복). env/세션 없으면 no-op.
- Modify `.gitignore` — `.env.local` 확인(이미 있을 수 있음).

> UI(로그인 버튼·세션·MY·저널 화면)와 store mutation에 sync 훅을 거는 작업은 **P1.1/P1.2**. 본 계획은 토대(스키마·클라이언트·병합·원격 왕복)까지만.

---

### Task 1: 의존성 + env 가드된 Supabase 클라이언트 + .env.example

**Files:**
- Create: `src/lib/supabase/client.ts`, `src/lib/supabase/server.ts`, `.env.example`
- Modify: `package.json` (deps)

**Interfaces:**
- Produces:
  - `getBrowserSupabase(): SupabaseClient | null` — env 없으면 null.
  - `getServerSupabase(): Promise<SupabaseClient | null>` — App Router 쿠키 기반. env 없으면 null.

- [ ] **Step 1: 의존성 설치**

Run:
```bash
npm install @supabase/supabase-js@^2 @supabase/ssr@^0.5
```
Expected: 두 패키지가 dependencies에 추가.

- [ ] **Step 2: 브라우저 클라이언트**

Create `src/lib/supabase/client.ts`:
```ts
import { createBrowserClient } from "@supabase/ssr";
import type { SupabaseClient } from "@supabase/supabase-js";

const url = process.env.NEXT_PUBLIC_SUPABASE_URL;
const anon = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;

/** env가 없으면 null → 앱은 게스트 모드로 계속 동작한다. */
export function getBrowserSupabase(): SupabaseClient | null {
  if (!url || !anon) return null;
  return createBrowserClient(url, anon);
}

export const isSupabaseConfigured = Boolean(url && anon);
```

- [ ] **Step 3: 서버 클라이언트**

Create `src/lib/supabase/server.ts`:
```ts
import { cookies } from "next/headers";
import { createServerClient } from "@supabase/ssr";
import type { SupabaseClient } from "@supabase/supabase-js";

const url = process.env.NEXT_PUBLIC_SUPABASE_URL;
const anon = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;

/** App Router 쿠키 기반 서버 클라이언트. env 없으면 null. */
export async function getServerSupabase(): Promise<SupabaseClient | null> {
  if (!url || !anon) return null;
  const store = await cookies();
  return createServerClient(url, anon, {
    cookies: {
      getAll: () => store.getAll(),
      setAll: (list) => {
        try {
          for (const { name, value, options } of list) {
            store.set(name, value, options);
          }
        } catch {
          // called from a Server Component — safe to ignore (middleware refreshes)
        }
      },
    },
  });
}
```

- [ ] **Step 4: .env.example**

Create `.env.example`:
```
# Supabase (P1). 값은 .env.local 에만 넣고 커밋하지 말 것.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
```

- [ ] **Step 5: 빌드·테스트 확인 (env 없이도 그린)**

Run:
```bash
npx tsc --noEmit
npm run test
npm run build
```
Expected: tsc 0 에러(신규 모듈 기준), 테스트 그린, `next build` 성공. env 없이 게스트 모드 유지.

- [ ] **Step 6: 커밋**

```bash
git add package.json package-lock.json src/lib/supabase .env.example
git commit -m "Add env-guarded Supabase browser/server clients"
```

---

### Task 2: DB 스키마 마이그레이션 (스키마 + RLS + 신규유저 트리거)

**Files:**
- Create: `supabase/migrations/0001_p1_foundation.sql`

**Interfaces:**
- Produces(테이블): `profiles`, `readings`, `collection`, `journal_entries` (모두 RLS로 소유자 격리).

- [ ] **Step 1: 마이그레이션 SQL 작성**

Create `supabase/migrations/0001_p1_foundation.sql`:
```sql
-- P1 foundation: 프로필 · 리딩 · 도감 · 일별 기록. 모두 RLS로 소유자만 접근.

create table if not exists public.profiles (
  id uuid primary key references auth.users(id) on delete cascade,
  created_at timestamptz not null default now(),
  display_name text,
  selected_deck_id text not null default 'classic'
);

create table if not exists public.readings (
  id uuid primary key, -- 클라 생성 uuid(P0-a). upsert 멱등키.
  user_id uuid not null references auth.users(id) on delete cascade,
  created_at timestamptz not null default now(),
  local_date date not null,
  iso_week text not null,
  spread text not null,
  type_id text not null,
  category text not null,
  deck_id text not null,
  cards jsonb not null,
  orientations jsonb not null
);
create index if not exists readings_user_idx on public.readings (user_id, local_date);

create table if not exists public.collection (
  user_id uuid not null references auth.users(id) on delete cascade,
  deck_id text not null,
  slug text not null,
  first_at timestamptz not null,
  count int not null default 1,
  primary key (user_id, deck_id, slug)
);

create table if not exists public.journal_entries (
  user_id uuid not null references auth.users(id) on delete cascade,
  entry_date date not null,
  body text not null default '',
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (user_id, entry_date)
);

-- RLS
alter table public.profiles enable row level security;
alter table public.readings enable row level security;
alter table public.collection enable row level security;
alter table public.journal_entries enable row level security;

create policy "own profile" on public.profiles
  for all using (auth.uid() = id) with check (auth.uid() = id);
create policy "own readings" on public.readings
  for all using (auth.uid() = user_id) with check (auth.uid() = user_id);
create policy "own collection" on public.collection
  for all using (auth.uid() = user_id) with check (auth.uid() = user_id);
create policy "own journal" on public.journal_entries
  for all using (auth.uid() = user_id) with check (auth.uid() = user_id);

-- 신규 가입 시 profiles 행 자동 생성
create or replace function public.handle_new_user()
returns trigger language plpgsql security definer set search_path = public as $$
begin
  insert into public.profiles (id) values (new.id) on conflict do nothing;
  return new;
end;
$$;

drop trigger if exists on_auth_user_created on auth.users;
create trigger on_auth_user_created
  after insert on auth.users
  for each row execute function public.handle_new_user();
```

- [ ] **Step 2: (사용자) 적용 확인**

이 SQL은 Supabase SQL Editor 또는 `supabase db push`로 사용자가 적용한다. 코드 검증 대상 아님 — 파일 존재와 문법 정합성만 리뷰.

- [ ] **Step 3: 커밋**

```bash
git add supabase/migrations/0001_p1_foundation.sql
git commit -m "Add P1 DB schema migration (profiles, readings, collection, journal + RLS)"
```

---

### Task 3: 병합 로직 (순수, TDD)

**Files:**
- Create: `src/lib/sync/merge.ts`
- Test: `src/lib/sync/merge.test.ts`

**Interfaces:**
- Consumes: `ArcanaStore`, `ReadingRecord`, `CollectionEntry` (`@/lib/store`).
- Produces:
  - `recomputeCollection(readings: ReadingRecord[]): ArcanaStore["collection"]` — 리딩에서 덱별 도감 재계산(firstAt=최초 at, count=등장 수).
  - `mergeStores(a: ArcanaStore, b: ArcanaStore): ArcanaStore` — 리딩 union(id), 도감 재계산.

- [ ] **Step 1: 실패 테스트 작성**

Create `src/lib/sync/merge.test.ts`:
```ts
import { describe, expect, it } from "vitest";
import type { ArcanaStore, ReadingRecord } from "@/lib/store";
import { mergeStores, recomputeCollection } from "@/lib/sync/merge";

function rec(over: Partial<ReadingRecord>): ReadingRecord {
  return {
    id: "r1",
    at: "2026-07-19T05:00:00.000Z",
    localDate: "2026-07-19",
    isoWeek: "2026-W29",
    spread: "one",
    typeId: "ONE_CARD",
    category: "love",
    deckId: "classic",
    cards: ["thefool"],
    orientations: ["upright"],
    ...over,
  };
}

describe("recomputeCollection", () => {
  it("덱별로 등장 수와 최초 at을 집계한다", () => {
    const readings = [
      rec({ id: "a", at: "2026-07-19T05:00:00.000Z", cards: ["thefool"] }),
      rec({ id: "b", at: "2026-07-20T05:00:00.000Z", cards: ["thefool", "themoon"] }),
      rec({ id: "c", at: "2026-07-19T05:00:00.000Z", deckId: "wolha-biwon", cards: ["thefool"] }),
    ];
    const col = recomputeCollection(readings);
    expect(col.classic.thefool).toEqual({ firstAt: "2026-07-19T05:00:00.000Z", count: 2 });
    expect(col.classic.themoon.count).toBe(1);
    expect(col["wolha-biwon"].thefool.count).toBe(1);
  });
});

describe("mergeStores", () => {
  it("리딩을 id로 합치고(중복 제거) 도감을 재계산한다", () => {
    const a: ArcanaStore = {
      version: 2,
      collection: {},
      readings: [rec({ id: "shared", cards: ["thefool"] })],
    };
    const b: ArcanaStore = {
      version: 2,
      collection: {},
      readings: [
        rec({ id: "shared", cards: ["thefool"] }), // 중복 → 1회만
        rec({ id: "extra", at: "2026-07-21T00:00:00.000Z", cards: ["themoon"] }),
      ],
    };
    const merged = mergeStores(a, b);
    expect(merged.readings).toHaveLength(2);
    expect(merged.collection.classic.thefool.count).toBe(1);
    expect(merged.collection.classic.themoon.count).toBe(1);
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `npm run test -- src/lib/sync/merge.test.ts`
Expected: FAIL — 모듈 없음.

- [ ] **Step 3: 구현**

Create `src/lib/sync/merge.ts`:
```ts
import type { ArcanaStore, CollectionEntry, ReadingRecord } from "@/lib/store";

/** 리딩에서 덱별 도감을 재계산한다(firstAt=최초 at, count=등장 수). */
export function recomputeCollection(
  readings: ReadingRecord[],
): ArcanaStore["collection"] {
  const byAt = [...readings].sort((x, y) => x.at.localeCompare(y.at));
  const collection: ArcanaStore["collection"] = {};
  for (const r of byAt) {
    const deck = (collection[r.deckId] ??= {} as Record<string, CollectionEntry>);
    for (const slug of r.cards) {
      const entry = deck[slug];
      deck[slug] = entry
        ? { firstAt: entry.firstAt, count: entry.count + 1 }
        : { firstAt: r.at, count: 1 };
    }
  }
  return collection;
}

/** 두 스토어를 병합: 리딩 union(id 기준), 도감은 재계산으로 정합성 보장. */
export function mergeStores(a: ArcanaStore, b: ArcanaStore): ArcanaStore {
  const byId = new Map<string, ReadingRecord>();
  for (const r of [...a.readings, ...b.readings]) {
    if (!byId.has(r.id)) byId.set(r.id, r);
  }
  const readings = [...byId.values()];
  return { version: 2, collection: recomputeCollection(readings), readings };
}
```

- [ ] **Step 4: 통과 확인**

Run: `npm run test -- src/lib/sync/merge.test.ts`
Expected: PASS (2 tests).

- [ ] **Step 5: 커밋**

```bash
git add src/lib/sync/merge.ts src/lib/sync/merge.test.ts
git commit -m "Add pure store merge + collection recompute for sync"
```

---

### Task 4: 원격 왕복 (pull/push, env·세션 없으면 no-op)

**Files:**
- Create: `src/lib/sync/remote.ts`

**Interfaces:**
- Consumes: `getBrowserSupabase` (Task 1); `ArcanaStore`, `ReadingRecord` (`@/lib/store`); `mergeStores` (Task 3).
- Produces:
  - `pullRemoteStore(userId: string): Promise<ArcanaStore | null>` — 서버에서 이 유저의 readings를 읽어 스토어 형태로 반환. 클라이언트 없으면 null.
  - `pushLocalStore(userId: string, store: ArcanaStore): Promise<void>` — readings를 upsert(멱등). 클라이언트 없으면 no-op.

> 세션 획득(로그인)과 이 함수들을 store mutation·로그인 이벤트에 연결하는 작업은 P1.1. 본 태스크는 순수 왕복 함수까지.

- [ ] **Step 1: 구현**

Create `src/lib/sync/remote.ts`:
```ts
import { getBrowserSupabase } from "@/lib/supabase/client";
import type { ArcanaStore, ReadingRecord } from "@/lib/store";
import { recomputeCollection } from "@/lib/sync/merge";

type ReadingRow = {
  id: string;
  created_at: string;
  local_date: string;
  iso_week: string;
  spread: string;
  type_id: string;
  category: string;
  deck_id: string;
  cards: string[];
  orientations: string[];
};

function rowToReading(r: ReadingRow): ReadingRecord {
  return {
    id: r.id,
    at: r.created_at,
    localDate: r.local_date,
    isoWeek: r.iso_week,
    spread: r.spread as ReadingRecord["spread"],
    typeId: r.type_id as ReadingRecord["typeId"],
    category: r.category,
    deckId: r.deck_id,
    cards: r.cards,
    orientations: r.orientations as ReadingRecord["orientations"],
  };
}

function readingToRow(userId: string, r: ReadingRecord): ReadingRow & { user_id: string } {
  return {
    user_id: userId,
    id: r.id,
    created_at: r.at,
    local_date: r.localDate,
    iso_week: r.isoWeek,
    spread: r.spread,
    type_id: r.typeId,
    category: r.category,
    deck_id: r.deckId,
    cards: r.cards,
    orientations: r.orientations,
  };
}

/** 서버에서 유저의 리딩을 읽어 스토어로. 미설정이면 null. */
export async function pullRemoteStore(userId: string): Promise<ArcanaStore | null> {
  const supabase = getBrowserSupabase();
  if (!supabase) return null;
  const { data, error } = await supabase
    .from("readings")
    .select("*")
    .eq("user_id", userId);
  if (error || !data) return null;
  const readings = (data as ReadingRow[]).map(rowToReading);
  return { version: 2, collection: recomputeCollection(readings), readings };
}

/** 로컬 리딩을 서버에 멱등 upsert. 미설정이면 no-op. */
export async function pushLocalStore(
  userId: string,
  store: ArcanaStore,
): Promise<void> {
  const supabase = getBrowserSupabase();
  if (!supabase || store.readings.length === 0) return;
  const rows = store.readings.map((r) => readingToRow(userId, r));
  await supabase.from("readings").upsert(rows, { onConflict: "id" });
}
```

- [ ] **Step 2: 확인 (env 없이 no-op 컴파일)**

Run:
```bash
npx tsc --noEmit
npm run test
npm run build
```
Expected: tsc 0 신규 에러, 테스트 그린, 빌드 성공(미설정 시 함수는 null/no-op).

- [ ] **Step 3: 커밋**

```bash
git add src/lib/sync/remote.ts
git commit -m "Add Supabase pull/push round-trip for readings (no-op when unconfigured)"
```

---

## Self-Review 메모

- **스펙 커버리지:** 백엔드 토대(스키마·클라이언트·병합·왕복)까지. 로그인 UI·세션·게스트→계정 병합 트리거·MY/저널 화면·store mutation sync 훅 = **P1.1/P1.2**.
- **RLS:** 4개 테이블 전부 소유자 정책. 신규유저 profiles 트리거 포함.
- **도감 정합성:** 병합 시 리딩 union 후 재계산 → 이중집계 불가.
- **env 가드:** 미설정 시 클라이언트 null·왕복 no-op → 게스트 모드로 빌드·구동 유지(검증 가능).
- **저널 병합:** 본 계획은 리딩 위주. 저널 테이블은 생성하되, 게스트 로컬 저널이 P1.2에서 생기면 병합 규칙(date별 updated_at 우선)을 그때 merge.ts에 추가.
- **외부 설정 의존:** 실제 로그인/동기화 검증은 사용자 Supabase·OAuth 설정 완료 후 P1.1에서 가능.
