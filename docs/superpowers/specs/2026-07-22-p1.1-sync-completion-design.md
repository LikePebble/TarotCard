# P1.1 마무리: 변경 전파 · 저널 동기화 · 로그아웃 설계

- 작성일: 2026-07-22
- 브랜치: `redesign/arcana`
- 상위 문서: [`2026-07-19-arcana-ia-design.md`](2026-07-19-arcana-ia-design.md) (D1 게스트 우선), [`2026-07-19-p1.0-supabase-foundation.md`](../plans/2026-07-19-p1.0-supabase-foundation.md)
- 범위: `7e6af41`(P1.1 auth 골격)이 남긴 follow-up 3건 — **push-on-mutation**, **저널 서버 동기화**, **로그인 병합 후 UI 갱신**.

## 1. 배경

P1.0에서 스키마·클라이언트·`mergeStores`·`pullRemoteStore`/`pushLocalStore`를, P1.1에서 세션·OAuth 콜백·`syncOnLogin`을 만들었다. 그러나 실제로 데이터가 흐르지 않는다:

1. **`pushLocalStore`를 아무도 호출하지 않는다.** `syncOnLogin` 안에서 단 한 번뿐이라, 로그인 이후에 뽑은 리딩은 서버에 영영 올라가지 않는다.
2. **저널은 원격 경로 자체가 없다.** `journal_entries` 테이블은 있지만 `pullRemoteJournal`/`pushLocalJournal`이 없다.
3. **`useArcanaStore`는 마운트 시 1회만 읽는다.** `refresh`를 호출하는 곳이 없어, `AccountCard`가 로그인 병합을 끝내도 열려 있는 화면은 옛 값을 계속 보여준다.

세 결손은 개별 버그가 아니라 **"로컬 저장이 바뀌었다"는 사실을 전파할 통로가 없다**는 하나의 결손이다. 본 설계는 그 통로를 만들고 세 문제를 동시에 닫는다.

## 2. 확정된 설계 결정 (Decision Log)

| # | 결정 | 값 |
|---|---|---|
| S1 | 동기화 목표 | ~~**계정 백업.** 한 번에 한 기기를 쓴다고 가정한다. 멀티 디바이스 동시 사용(실시간 구독·포그라운드 재동기화)은 범위 밖~~ **→ 2026-07-28 개정: S1a** |
| S1a | 동기화 목표 (개정) | **여러 기기에서 이어 보기.** 실시간 구독은 여전히 범위 밖이지만, **포그라운드 재동기화는 범위 안**이다 |
| S2 | 로컬 권위 | ~~로그인 이후 **로컬이 권위**다. 서버는 복구용 사본. pull은 로그인 시점 1회~~ **→ 2026-07-28 개정: S2a** |
| S2a | 로컬 권위 (개정) | 충돌 해소에서는 **로컬이 여전히 권위**다(당시 S3·S4 그대로 — 둘 다 이후 S3a·S4b로 개정됐다). 다만 **pull은 로그인 1회가 아니라** 탭 복귀·주기 갱신·네트워크 복귀에서도 돈다 |
| S3 | 저널 충돌 | ~~같은 날짜가 양쪽에 있으면 **`updatedAt`이 최신인 쪽**(LWW)~~ **→ 2026-07-28 개정: S3a** |
| S3a | 저널 충돌 (개정) | **이 기기가 이 계정과 처음 만나는 병합만 서버 우선**, 그 밖의 모든 병합은 LWW. 계정에 쌓인 기록이 이 기기에 우연히 남아 있던 게스트 기록보다 사용자가 기대하는 "내 일기"에 가깝다. 다만 갱신에서까지 서버를 우선하면 방금 이 기기에서 쓰고 아직 올라가지 못한 글을 주기 갱신(S9)이 서버의 옛 사본으로 되돌리므로, 그때는 LWW를 유지한다. 리딩은 이 규칙과 무관하다 — id를 가진 append-only 기록이라 union에 충돌이 없다 |
| S4 | 저널 삭제 | ~~톰스톤을 두지 않는다. push가 **로컬에 없는 서버 행을 삭제**해 삭제를 전파한다 (S2의 귀결)~~ **→ 2026-07-29 개정: S4b** |
| S4a | 삭제의 전제 | **이번 세션에서 pull이 성공했을 때만 삭제한다.** 서버에 무엇이 있는지 모르는 채로 지우면, 일시적 pull 실패가 서버 일기를 로컬 크기로 잘라낸다. pull이 실패한 세션에서는 올리기만 하고(upsert) 지우지 않는다 |
| S4b | 저널 삭제 (개정) | **톰스톤을 둔다.** 지운 날은 항목을 없애지 않고 **본문을 빈 문자열로** 남긴다. 삭제도 하나의 기록이 되어 일기와 같은 LWW(S3a)로 겨룬다. 스키마 변경은 없다 — `journal_entries.body`가 이미 `text not null default ''`다. 화면은 톰스톤을 "없는 날"로 보고(`isWritten`/`entryOf`/`writtenDates`), 동기화는 "지운 기록"으로 본다 |
| S5 | 로그아웃 | 대기 중 push를 flush한 뒤 **로컬 저장소를 비운다**. 공용 기기에서 남의 일기가 보이거나 다음 계정에 섞이는 사고를 막는다 |
| S5a | 로그아웃 중단 금지 | flush가 실패해도 **로그아웃은 반드시 진행**한다. 로그아웃은 사용자가 통제할 수 있어야 하는 동작이고, 네트워크가 없다고 계정에 갇히면 안 된다. 유실 범위는 마지막 성공 push 이후의 델타로 한정되며 그 이전 기록은 이미 서버에 있다 |
| S6 | 실패 처리 | 재시도 큐를 두지 않는다. push는 **멱등 upsert**라 다음 push가 밀린 것까지 복구한다. MY 계정 카드에 상태 한 줄만 노출 (~~전체 스토어~~ → S14에서 델타로 좁혔다. 실패하면 앎을 갱신하지 않으므로 멱등·복구 성질은 그대로다) |
| S6a | 변경 유실 금지 | S6의 "다음 push가 복구한다"는 **변경 알림이 하나도 버려지지 않을 때만** 성립한다. 동기화 중에 들어온 알림은 무시하는 게 아니라 `dirty` 플래그로 기억했다가, 진행 중이던 push가 끝나면 곧바로 다시 예약한다 |
| S6b | 실패는 반환값으로 | 원격 함수는 **던지지 않는다.** Supabase 클라이언트가 오류를 `{ error }`로 돌려주기 때문이다. 따라서 성공/실패는 `SyncOutcome`(`"ok" \| "failed" \| "skipped"`) 반환값으로 전달하고, 호출부는 `try/catch`가 아니라 그 값으로 상태를 정한다. pull도 "미설정"과 "실패"를 구분해야 S4a를 판단할 수 있다 |
| S7 | 전파 방식 | 저장 함수가 **키 있는 변경 이벤트**를 emit하고, 화면 훅과 `SyncBridge`가 각각 구독한다. 호출부에서 push를 직접 부르지 않는다 |
| S8 | 게스트 불변 | Supabase env가 없거나 세션이 없으면 `SyncBridge`는 즉시 빠져나간다. 게스트 경로에 네트워크가 끼어들지 않는다 |
| S9 | 갱신 시점 (2026-07-28) | 탭 복귀(`visibilitychange`·`focus`)·5분 주기·네트워크 복귀. **화면이 보이지 않으면 돌지 않는다** — 백그라운드 탭이 조용히 왕복을 쌓아 봐야 볼 사람이 없다 |
| S10 | 갱신 빈도 제한 (2026-07-28) | 병합 시작 기준 **최소 30초** 간격. 탭을 자주 오가도 왕복이 그만큼 늘지 않는다. 네트워크 복귀만 `force`로 건너뛴다. 진행 중인 동기화가 있으면 갱신은 그냥 건너뛴다(놓쳐도 유실이 아니라 다음 차례에 하면 되는 일이므로 `dirty`로 기억하지 않는다) |
| S11 | 세션 세대 (2026-07-28) | `epoch`은 **실제 세션 전이에서만** 올린다. Supabase는 한 번의 로그인에도 구독 즉시 `INITIAL_SESSION`, 뒤이어 세션 조회 응답, 이후 토큰 갱신으로 여러 번 통지하는데, 그때마다 세대를 올리면 진행 중인 병합이 **자기 자신 때문에** 스테일이 되어 pull 결과를 통째로 버린다 |
| S12 | 중단된 병합 (2026-07-28) | 스테일·예외로 아무것도 반영하지 못한 병합은 `mergedFor`를 되돌려 다시 시도할 수 있게 한다. 이미 다른 사용자로 넘어갔으면 그쪽 표시는 건드리지 않는다 |
| S13 | 헛 알림 금지 (2026-07-28) | 병합 결과가 로컬과 같으면 **로컬에 쓰지 않는다.** 쓰기는 `local-events`를 울리고 그 알림이 다시 push를 예약하므로, 그냥 두면 갱신마다 헛 push가 한 번씩 따라붙는다 |
| S14 | 델타 push (2026-07-28) | push는 **서버에 아직 없다고 아는 것만** 올린다. 리딩은 앎에 없는 id만, 일기는 `updatedAt`이 다른 날짜만. 일기 정리도 **지울 날짜만 짚어** 지운다(`in`), 앎이 없을 때만 종전의 전체 나열(`not.in`)로 물러선다. 셋 다 비면 왕복 자체가 사라진다 |
| S15 | 서버 앎의 수명 (2026-07-28) | 그 앎(`server-knowledge.ts`)은 **메모리에만** 둔다. 새로고침하면 pull이 먼저 돌아 다시 채워지고, 저장해 두면 서버와 어긋난 표식이 살아남아 "이미 올렸다"고 착각한 기록이 영영 올라가지 않는다. 모를 때의 동작이 항상 "전부 올린다"라서 **앎이 없어지는 쪽으로 틀려도 안전하다** — 반대 방향만 위험하다. 계정이 바뀌거나 로그아웃하면 버린다 |
| S16 | 병합 왕복 병렬화 (2026-07-28) | 리딩·일기·엔타이틀먼트는 서로 독립이므로 **함께 띄운다.** 각 갈래 안의 pull→push 순서는 그대로다. 엔타이틀먼트 pull은 `userId`를 인자로 받는다 — 예전에는 호출자가 아는 값을 두고 `auth.getUser()`로 왕복을 한 번 더 냈다 |
| S17 | "최초"를 재는 자 (2026-07-28) | S3a의 최초 판정은 **localStorage 표식**(`first-merge.ts`)으로 한다. 모듈 상태(`mergedFor`)로 재면 새로고침마다 다시 최초가 되어, 세션 복원 때의 로컬 — 게스트 잔재가 아니라 **같은 계정의 가장 최신 기록** — 이 서버 옛 사본으로 덮인다. 앎(S15)과 달리 저장이 안전한 이유는 방향이 반대여서다: 표식이 남는 쪽으로 틀리면 LWW로 물러설 뿐이고, 사라지는 쪽으로 틀려도 게스트 병합을 한 번 더 할 뿐이다. **명시적 로그아웃에서만 지운다** — 그때 로컬도 비우므로 다음 로그인은 진짜 게스트 병합이다. 세션 만료로 끊긴 경우에는 남긴다 |
| S18 | 로그아웃 경계 (2026-07-28) | flush는 반환하며 **sync 세션을 내려놓는다**(세대만 올리면 그 뒤 *새로* 시작된 갱신은 스테일이 아니라 비운 로컬을 되살린다). flush는 `dirty`로 삼킨 변경도 **마저 올린다**(재예약 타이머는 곧 도착할 `setSyncUser(null)`이 죽인다). `signOut()`이 오류를 돌려줘도 **로컬은 비운다**(auth-js는 revoke 실패에도 로컬 세션을 지운 뒤 오류를 반환한다). 로그아웃 뒤 뒤늦게 끝난 push는 **상태를 쓰지 않는다** |

### S1·S2를 개정한 이유 (2026-07-28)

원문을 남겨 둔다. **이 결정 자체가 틀렸다기보다, 결정 이후에 서비스가 사용자에게
다른 약속을 하면서 문서끼리 어긋났다.**

- `src/data/legal/terms.ts` 제4조: "계정 연동: 로그인하신 경우 위 기록을 계정에
  보관하여 **여러 기기에서 이어 볼 수 있게** 합니다."
- 같은 파일 제7조: "기록을 오래 보관하고 **여러 기기에서 이어 보기를** 원하시는
  경우 로그인해 주시기 바랍니다."
- `src/data/legal/privacy.ts`: 개인정보 이용 목적에 "**여러 기기에서 같은 기록을
  이어 볼 수 있도록 하는 동기화**"가 들어 있다.

S1의 "한 번에 한 기기" 가정 아래에서는 pull이 로그인 시점 1회뿐이라, B 기기에서
남긴 기록이 이미 열려 있는 A 기기에 나타나지 않는다. 약관이 이미 게시된 이상
코드가 약속을 따라가는 편이 맞다고 보아 포그라운드 재동기화를 범위 안으로 옮겼다.

실시간 구독(Realtime)은 여전히 범위 밖이다. 탭 복귀·주기 갱신으로 충분하고,
연결을 상시로 물고 있을 만큼의 값어치가 없다.

S4a(pull 성공 시에만 prune)는 **그대로 유지한다.** S4(톰스톤 없음)는 이 개정이
드러낸 대가를 감당하지 못해 **2026-07-29에 S4b로 뒤집혔다** — 아래 §5를 볼 것.

S3(LWW)은 S3a로 갈랐다. **알려진 대가:** 이 기기가 이 계정과 처음 만나는 병합에서
같은 날짜가 양쪽에 있으면, 게스트로 쓴 글이 **알림 없이 서버 것으로 대체된다.**
사용자가 의도한 동작이고(계정 기록이 정본), 게스트가 일기를 쓴 바로 그 날짜에
서버에도 일기가 있어야 성립하므로 드물다. 화면에 알림을 띄우려면 병합 결과를
UI까지 전달할 통로가 필요한데 지금은 그런 통로가 없다 — 필요해지면 그때 만든다.

**이 "드물다"는 S17이 있어야만 참이다.** 최초를 모듈 상태로 재면 세션 복원마다
서버 우선이 되고, 그때 덮이는 것은 게스트 잔재가 아니라 **로그인한 사용자가 방금
저장한 글**이다. 저장 버튼을 누르고 2초 디바운스가 발화하기 전에 탭을 닫기만 하면
되는, 전혀 드물지 않은 순서다. S3a를 손댈 때 S17을 함께 보라.

### S7을 택한 이유

대안은 호출부에서 `recordReading()` 뒤에 push를 명시 호출하는 것이었다. diff는 작지만 (a) 새 저장 경로가 생길 때 빠뜨리면 **조용히 백업이 새고**, (b) UI 갱신은 여전히 별도 메커니즘이 필요해 결국 두 갈래가 된다. 이벤트 방식은 저장하면 반드시 흐르므로 P2에서 사주 리딩 저장 경로가 추가돼도 누락이 구조적으로 불가능하다.

## 3. 아키텍처

새로 생기는 조각은 3개다.

```
src/lib/local-events.ts     변경 이벤트 채널 (~25줄, 의존성 없음)
src/components/SyncBridge   layout에 1개. 세션 관찰 + 디바운스 push + 상태 노출
src/lib/sync/journal-remote.ts  저널 pull/push
```

2026-07-28 추가: `src/lib/sync/server-knowledge.ts` — 서버에 무엇이 있는지에 대한
이 세션의 앎(S14·S15). pull이 채우고 push가 갱신한다. 메모리에만 있고 다른 모듈을
import하지 않는다.

의존 방향은 한 방향으로 유지한다:

```
store.ts / journal.ts  ──emit──▶  local-events.ts  ◀──subscribe──  useArcanaStore / useJournal
                                        ▲
                                        └──subscribe──  SyncBridge ──▶ sync/*  ──▶ supabase/*
```

`store.ts`·`journal.ts`는 **Supabase를 import하지 않는다.** 로컬 우선 원칙(P1.0 Global Constraints)이 그대로 유지되고, 두 파일의 유닛테스트도 네트워크와 무관하게 남는다.

### 3.1 `local-events.ts`

```ts
export type LocalChannel = "store" | "journal";
export function subscribeLocal(ch: LocalChannel, fn: () => void): () => void;
export function notifyLocal(ch: LocalChannel): void;
```

채널을 나누는 이유는 일기 저장이 도감 화면을 리렌더시키지 않게 하기 위함이다. SSR 안전(모듈 스코프 `Map`, 브라우저 전용 API 없음).

### 3.2 emit 지점

`store.ts`의 `saveStore`, `journal.ts`의 `saveJournal` **내부** 끝에서 emit한다. 두 함수는 이미 모든 쓰기(`recordReading`, `setLocalStore`, `setEntry`)가 지나는 단일 통로이므로, 여기 한 곳만 잡으면 새는 경로가 없다.

### 3.3 구독 지점

- `useArcanaStore` / `useJournal`: 마운트 시 해당 채널을 구독해 `refresh`를 건다. 기존 `refresh` 반환값은 유지(호환).
- `SyncBridge`: 두 채널을 모두 구독한다.

### 3.4 `SyncBridge`

`layout.tsx`의 `<body>`에 `<NoPinchZoom />` 옆에 놓는 클라이언트 컴포넌트. UI를 렌더하지 않는다(`null`).

책임:
1. `useSession()`으로 `user: null → {id}` 전이를 감지해 서버 병합 실행. (지금 `AccountCard`가 갖고 있는 이 로직을 여기로 **옮긴다** — MY 화면을 열어야만 병합되는 현재 동작은 버그다.)
   - 2026-07-28: 함수명이 `reconcileStore`/`reconcileJournal`로 바뀌었다. 로그인 전용이 아니라 갱신도 같은 경로를 쓰기 때문이다(S1a·S2a).
2. 변경 이벤트를 받아 **2초 디바운스** 후 로그인 상태면 push.
3. ~~`window` `online` 이벤트에 push를 건다(오프라인 복귀 복구).~~ **→ 2026-07-28:** 복귀·포커스·주기·`online`에 **갱신**을 건다(S9). `online`은 `refreshFromRemote({force:true})`를 먼저 쓰고, 그것이 건너뛰어졌을 때만 push를 예약한다. 배선은 `src/lib/sync/refresh-triggers.ts`에 있다 — 컴포넌트 안에 두면 틀려도 화면에 증상이 없어 시험할 수 없다.
4. 동기화 상태를 노출한다.

## 4. 데이터 흐름 시나리오

### 게스트 (env 없음 또는 미로그인)
`recordReading()` → `saveStore()` → `notifyLocal("store")` → 화면 갱신. `SyncBridge`는 세션이 없어 무시. **현행과 동일하며 네트워크 호출이 없다.**

### 첫 로그인 (게스트 기록 보유)
```
OAuth → /auth/callback → 세션 확립
  → SyncBridge가 전이 감지
  → 세 갈래를 함께 띄운다(S16). 각 갈래 안에서만 pull → push 순서:
      reconcileStore(id):   pull → mergeStores          → setLocalStore*   → push(델타)
      reconcileJournal(id): pull → mergeJournals(remote) → setLocalJournal* → push(델타)
      pullRemoteEntitlements(id)                                              (pull만)
       * 바뀐 게 있을 때만 쓴다(S13). 쓰면 notify가 나가 열려 있는 화면이 갱신된다.
         conflict가 remote인 것은 **이 기기가 이 계정과 처음 만날 때뿐**이다(S3a).
```

### 로그인 상태에서 저장
`recordReading()` / `setEntry()` → notify → ①화면 갱신 ②`SyncBridge` 디바운스 2초 → push.

### 오프라인·실패
push 실패 → 상태 `error` → MY에 `동기화 대기 중`. 로컬은 이미 저장돼 앱은 정상 동작. `online` 복귀 또는 다음 저장 시 복구된다. S14 이후 push는 델타지만, **실패하면 앎을 갱신하지 않으므로**(S15) 다음 push가 밀린 분을 그대로 다시 집는다 — 멱등·복구 성질은 유지된다.

### 로그아웃
```
signOutAndClear():
  대기 중 push flush(3초 타임아웃, 실패해도 계속)
    → flush가 반환하며 sync 세션을 내려놓는다(currentUserId=null, epoch++)
    → supabase.auth.signOut()
    → **성공 여부와 무관하게** localStorage 비움
       (리딩·일기·엔타이틀먼트·병합 표식·arcana.sync.lastAt) → 상태 idle
    → notify → 화면 리셋
```

**flush가 세션을 내려놓는 이유(2026-07-28):** epoch만 올리면 그 뒤에 *새로* 시작된
갱신은 새 세대를 캡처하므로 스테일이 아니다. `signOut()`의 네트워크 왕복 중에 주기
틱이 발화하면 그 pull이 방금 비운 로컬에 이전 계정 데이터를 되살린다.

**flush가 dirty를 마저 올리는 이유:** 진행 중이던 작업 도중에 들어온 저장은 그
작업의 스냅샷에 없고 `dirty`로만 기억된다. `endSync`가 거는 2초 재예약은 곧 도착할
`setSyncUser(null)`이 죽이므로, flush가 알면서 빠뜨리고 성공 반환한 꼴이 된다.
S5a가 수용한 것은 "flush **실패** 시의 델타"까지다.

**signOut이 오류를 돌려줘도 비우는 이유:** auth-js는 서버 revoke가 네트워크 오류·
5xx로 실패해도 로컬 세션을 지우고 `SIGNED_OUT`을 방출한 뒤 오류를 반환한다
(`GoTrueClient._signOut`). 그때 로컬을 남기면 화면은 게스트인데 이전 사용자의
기록이 남고, 다음 계정의 로그인 병합이 **그것을 자기 계정으로 올린다.** 반대
방향(불필요하게 비움)은 서버 사본에서 되돌아온다.
`lastAt`까지 지우는 이유는 S5와 같다 — 지우지 않으면 다음 계정이 아직 아무것도 올리지 않았는데 이전 사용자의 `마지막 동기화 HH:MM`을 본다.
flush 성공 여부와 무관하게 로그아웃은 완주한다(S5a). 재로그인 시 pull로 복구된다.

## 5. 저널 원격 계층

`journal_entries` 스키마: `(user_id, entry_date)` PK, `body`, `updated_at`.

```ts
// src/lib/sync/merge.ts (순수 · 테스트 대상)
export function mergeJournals(local, remote, policy: "remote" | "newer"): JournalStore;
//   한쪽에만 있으면 그대로 채택. 양쪽에 있을 때만 policy로 가른다(S3a):
//     "remote" — 서버. 이 기기가 이 계정과 **처음 만날 때만**.
//     "newer"  — updatedAt이 큰 쪽, 같으면 local. 그 밖의 모든 병합.
//   policy도 prune과 같은 이유로 **선택 인자가 아니다.**

// src/lib/sync/outcome.ts
export type SyncOutcome = "ok" | "failed" | "skipped";           // S6b
export type PullResult<T> = { outcome: "ok"; data: T } | { outcome: "failed" | "skipped" };

// src/lib/sync/journal-remote.ts
export function pullRemoteJournal(userId): Promise<PullResult<JournalStore>>;
export function pushLocalJournal(userId, store, options: { prune: boolean }): Promise<SyncOutcome>;
//   1) 로컬 전 항목 upsert (onConflict: "user_id,entry_date")
//   2) options.prune일 때만 로컬에 없는 서버 행 삭제 (S4·S4a)
```

`prune`은 **선택 인자가 아니라 필수**다. 기본값을 두면 호출부가 실수로 삭제를 켤 수 있고, 그 실수의 대가가 서버 데이터 소실이다.

`pushLocalJournal`의 순서는 **upsert 먼저**다. delete가 먼저면 그 사이 실패했을 때 서버에 아무것도 없는 창이 생긴다. ~~두 번의 왕복을 쓴다~~ **→ 2026-07-28:** S14 이후 0~2회다. 올릴 것도 지울 것도 없으면 왕복이 통째로 사라진다.

**로컬이 빈 경우 delete를 실행하지 않는다.** `loadJournal()`은 localStorage 파손·파싱 실패 시에도 `{}`를 반환하므로, 빈 로컬을 그대로 신뢰하면 파손 한 번이 서버 백업 전체를 지운다. 백업이 존재 이유인 기능에서 이 위험은 받아들일 수 없다.

대가는 **일기를 전부 지웠을 때(마지막 한 개 삭제 포함) 그 삭제가 즉시 전파되지 않는다**는 것이다. 다음에 아무 일기나 하나 쓰면 그때 정리된다. ~~톰스톤이 필요해지는 시점(=멀티 디바이스, S1 범위 밖)에 함께 해결한다.~~

**→ 2026-07-28 정정.** S1a가 멀티 디바이스를 범위 안으로 옮겼으므로 위 괄호는
자기모순이고, 실제 대가도 위 서술보다 크다. **개별 삭제가 기기 사이에서 부활한다:**

1. 기기 A가 일기 `d`를 지운다 → A의 push가 서버에서 `d`를 지운다.
2. `d`를 아직 들고 있는 기기 B가 갱신한다 → `mergeJournals`는 한쪽에만 있는 날짜를
   그대로 채택하므로(policy와 무관) **`d`가 B의 로컬에 남는다.**
3. B의 다음 push는 `d`를 "앎에 없는 날짜"로 보고 **서버에 다시 올린다.**
4. A가 다시 갱신하면 2번과 같은 이유로 **A의 로컬에도 되살아난다.**

즉 지워지지 않는 정도가 아니라 양쪽에서 **되살아난다.** 삭제가 살아남는 경우는
다른 기기가 다시는 동기화하지 않을 때뿐이다.

### → 2026-07-29 해결: 톰스톤 (S4b)

지운 날을 **없애는 대신 빈 본문으로 남긴다.** 그러면 "서버에 없다"와 "지웠다"가
구분되고, 삭제가 일기와 같은 자격으로 LWW에 참여한다.

- **스키마 변경 없음.** `body text not null default ''`가 이미 빈 문자열을 받는다.
- **판정자를 새로 만들지 않는다.** 삭제 시각 대 수정 시각을 S3a가 그대로 가른다.
  삭제가 더 최신이면 삭제가 이기고, 다른 기기의 수정이 더 최신이면 수정이 이긴다
  (삭제 후 다시 쓴 글이 지워지지 않는다).
- **새로고침·기기 종료와 무관하다.** 사실이 서버에 남아 있으므로, 앎(S15)이
  메모리 전용인 것과 상관없이 동작한다.
- **행이 쌓이지만 하루 1행 상한**이라 양이 문제되지 않는다.

로컬에서는 `journal.ts`가 톰스톤을 들고, 화면은 `isWritten`/`entryOf`/`writtenDates`로
거른다. 판정을 이 셋에 모은 이유는 새 화면이 `store[date]`를 그냥 진위 판정하다가
지운 날을 보여주는 사고를 막기 위해서다.

**남는 한계:** 톰스톤 이전에 지운 기록은 로컬에 흔적이 없다. 그 날짜는 서버에서
한 번 되살아난 뒤에야 정상 경로를 탄다(다시 지우면 그때부터 톰스톤이 된다).
기존 사용자에게 한 번 일어나고 끝나는 일이라 마이그레이션을 따로 두지 않았다.

`prune`(로컬에 없는 서버 행 삭제)은 **남겨 두되 사실상 마이그레이션 경로**가 됐다.
톰스톤 이후 지운 날짜는 로컬에 남으므로 `removed`에 걸리지 않는다.

## 6. 오류 처리

| 상황 | 처리 |
|---|---|
| env 미설정 | 모든 원격 함수가 `"skipped"`를 반환한다. `SyncBridge`도 즉시 반환 |
| 세션 없음 | push 시도 자체를 하지 않음 |
| pull 실패 | 로컬을 그대로 두고 상태 `error`. **그리고 그 세션 동안 저널 prune을 끈다**(S4a) |
| push 실패 | 원격 함수가 `console.error` 후 `"failed"`를 **반환**한다(던지지 않는다, S6b). 호출부가 그 값으로 상태 `error`를 세운다. 다음 변경/`online`에서 자동 재시도 |
| 로그인 직후 중복 push | 진행 중 작업 수(`inFlight`)로 자기 자신이 유발한 notify를 무시하되, **그 사이 들어온 사용자 저장은 `dirty`로 기억해 작업이 끝나면 다시 예약**한다(S6a). 불리언 플래그가 아니라 카운터인 이유는, 겹친 두 작업 중 먼저 끝난 쪽이 나머지의 플래그까지 내려버리기 때문이다 |
| 로그아웃과 병합의 경합 | `epoch` 카운터를 둔다. 진행 중이던 병합은 로컬에 쓰기 직전 자기 epoch가 유효한지 확인한다. 이게 없으면 로그아웃이 비운 로컬을 뒤늦게 끝난 병합이 되살려 S5가 깨진다. **epoch는 `setSyncUser`뿐 아니라 `flushPendingSync`가 반환할 때도 올린다** — 로그아웃 순서가 `flush → 로컬 비움 → signOut → (auth 이벤트) → setSyncUser(null)`이라, `setSyncUser`에서만 올리면 "비움"과 "setSyncUser" 사이에 끝난 병합이 그대로 로컬을 되살린다 |
| 로그아웃 중 flush 실패 | **로그아웃을 그대로 진행한다**(S5a). flush에 3초 타임아웃을 두어 네트워크가 죽어 있어도 로그아웃이 매달리지 않게 하고, 실패해도 로컬을 비우고 세션을 끊는다. 마지막 성공 push 이후의 델타는 유실된다 — S5(유출 방지)와 S5a(통제권)를 유실보다 우선한 결과다 |

동기화 상태는 `"idle" | "syncing" | "ok" | "error"` + `lastSyncedAt`(localStorage `arcana.sync.lastAt`, 새로고침 후에도 표시)으로 두고, MY `AccountCard`가 한 줄로 읽는다.

## 7. 테스트

Supabase 없이 검증 가능한 부분에 유닛테스트를 집중한다(현행 vitest, 네트워크 모킹 없음).

- `mergeJournals` — 한쪽에만 존재 / 양쪽 존재 시 LWW / 동일 `updatedAt` 시 결정적 선택 / 빈 스토어.
- `local-events` — 구독·해제, 채널 격리(`journal` emit이 `store` 구독자를 깨우지 않음), 다중 구독자.
- `store.ts` — `saveStore`가 `notifyLocal("store")`를 부르는지(기존 17개 테스트는 그대로 통과해야 함).
- `journal.ts` — `setEntry`가 `notifyLocal("journal")`을 부르는지.
- **`pusher.ts`** — 원격 모듈을 `vi.mock`하고 가짜 타이머로: 게스트일 때 아무것도 예약하지 않음(S8), 디바운스 합치기, **진행 중 push 사이에 들어온 저장이 유실되지 않음**(S6a), push가 매달려도 `flushPendingSync`가 타임아웃 안에 반환, `"failed"` 반환 시 상태가 `error`(S6b), `pullOk: false`면 `prune: false`로 push(S4a).
- `journalPruneFilter` — 손으로 조립하는 PostgREST 필터 문자열의 인용 부호.

`pusher.ts`는 React·DOM 의존이 없어 node 환경에서 그대로 테스트된다. 애초에 순환 참조 때문에 컴포넌트에서 떼어낸 모듈인데, 결과적으로 **결함이 몰리는 곳**이기도 하므로 테스트 대상에서 빼면 안 된다.

2026-07-28 추가: `pusher.ts`에 **같은 사용자 재통지가 진행 중 병합을 스테일로
만들지 않음**(S11), 계정 전환·로그아웃은 여전히 스테일로 만듦, 예외로 끝난 병합의
재시도(S12), `refreshFromRemote`의 게스트·최소 간격·`force`·진행 중 건너뛰기(S9·S10).
`sync.ts`에 **변화가 없으면 로컬에 쓰지 않음**(S13)과 로그인/갱신의 충돌 규칙(S3a).
`remote.ts`·`journal-remote.ts`에 **앎이 없으면 전부·있으면 델타**, 지울 것이 없으면
왕복을 건너뜀, 실패 시 앎을 갱신하지 않아 다음 push가 다시 시도함(S14).
`server-knowledge.ts`에 계정 격리와 로그아웃 폐기(S15). `session.ts`의 세션 스토어는
훅과 분리해 모듈 API(`startAuth`/`getAuthSnapshot`/`subscribeAuth`)로 노출했고,
**같은 사용자 재통지 시 이전 객체를 그대로 두는 것**을 그 API로 시험한다 — React
컴포넌트를 유닛테스트하지 않는 규약을 지키면서 S11의 원인 쪽을 덮기 위해서다.

`SyncBridge`·실제 원격 왕복은 유닛테스트하지 않는다. Supabase 설정 완료 후 **수동 검증 체크리스트**로 확인한다: 게스트 기록 → 로그인 → 서버 행 확인 → 새 리딩 → 2초 후 서버 반영 → 일기 작성·삭제 반영 → 로그아웃 시 로컬 비움 → 재로그인 시 복구.

## 8. 범위 밖 (명시적 제외)

- 멀티 디바이스 실시간 동기화 (S1 → S1a에서도 유지). ~~포그라운드 재동기화~~는
  2026-07-28 S1a로 범위 안이 되었다
- 저널 삭제 톰스톤 (S4)
- 재시도 큐·백오프 (S6)
- `collection` 테이블 서버 반영 — 도감은 리딩에서 파생되므로 P1에서는 서버에 쓰지 않는다(P1.0 결정 유지)
- `profiles.selected_deck_id` 동기화 — 덱 선택은 기기 로컬 설정(P3 스토어에서 재검토)
- 사주·엔타이틀먼트 (P2/P3)
